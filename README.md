# lp5


Assignment 1 parallel bfs and dfs

Check if OpenMP is installed
g++ -fopenmp -o para parallel.cpp

if OpenMP does not exists
sudo apt update
sudo apt install libomp-dev

Always compile like this:
g++ -fopenmp parallel.cpp -o parallel
-----------------------------------------